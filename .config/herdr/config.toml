onboarding = false

[ui]
show_agent_labels_on_pane_borders = false

# Reclaim the scrollbar column so Shift+drag selections in the host terminal don't
# pick up the scrollbar glyphs.
pane_scrollbars = false

# Uncomment if border glyphs still end up inside selections.
# pane_outer_borders = false

[ui.sound]
enabled = false

[ui.sidebar.spaces]
rows = [['state_icon', 'workspace'], ['git_status', 'branch']]
row_gap = 1

[experimental]
pane_history = true
kitty_graphics = true
